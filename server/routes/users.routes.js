// user
const USER = "/users";

//logout
const LOGOUT = "/logout";

// jwt
const JWT = "/jwt";

// refresh
const REFRESH = "/refresh";

// seat
const SEAT = "/seat";

const userRoutes = {
  user: USER,
  logout: LOGOUT,
  refresh: REFRESH,
  jwt: JWT,
  seat: SEAT,
};

export default userRoutes;
